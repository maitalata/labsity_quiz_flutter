import 'package:flutter/material.dart';
import 'package:labsity_tech/quiz.dart';

class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(context) {
    return Quiz();
  }
}
